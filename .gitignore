node_modules/
vendor/
tests/.phpunit.cache/
.phpunit.result.cache
tests/e2e/**/.env
*.log

# Secret-scan reports produced by .github/workflows/gitleaks.yml
gitleaks-*.sarif

# Playwright run artefacts
test-results/

# Artefacts of a module-local `composer install`: oxideshop-ce is a dev
# dependency, so its package installer lays down a shop skeleton and creates
# directories named after the unresolved config placeholders.
/source/
/var/
/<sCompileDir>/
/<sShopDir>/
composer.lock
