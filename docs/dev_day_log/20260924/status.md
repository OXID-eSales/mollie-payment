# 2026-09-24

- Sprint MOL-18 (one order per checkout attempt — "Order now" clicked several times) — **DONE**, all 6 stories. Plan `../20260923/sprints/MOL-18-single-order-per-checkout-attempt.md`; `done/MOL-18-single-order-per-checkout-attempt.md`; `reports/MOL-18-single-order-per-checkout-attempt.md`.
  - Branches: mollie-payment `b-7.4.x-mol-10-18-single-click-and-order-state` (`d5ee7ed`, `fc9d87f`, `8825cec` + docs commit), payment-base `b-7.4.x-MOL-18-single-order-per-checkout-attempt` (`3e4e690`, `ca445b6`, `38b0bd4` + docs commit). payment-base merged into `b-7.4.x` (`4b59482`, no-ff, CI green); Mollie branch merged into `b-7.4.x` (no-ff) after CI green on `70d5bd5` — see `reports/MOL-18-merge-and-ci.md`.
  - Root cause confirmed by machine: click 2 retires attempt 1 (order storno'd) and core's `ORDEREXISTS` for the unchanged `sess_challenge` was treated as success → phantom order row (no articles, no payment type) got paid. 53 phantoms in the local DB before the sprint, 2 more from the Story 1 red proofs, **0 since Story 2**.
  - Fix: payment-base refuses the second `createOrder()` (`order_exists`), knows when an attempt is in flight (`InFlightCheckoutAttemptResolver`), rotates `sess_challenge` on retire, and now persists the provider redirect URL (was silently dropped — not in the plan); Mollie's `execute()` replays the in-flight redirect (`Service\InFlightCheckoutReplay`), and both order buttons submit once (`mollie-place-order` controller for the classic flow, in-flight guard in `mollie-components`).
  - Proof: e2e `OrderNowDoubleClickCreatesOneOrder` (2 tests) and `OrderRetryAfterUnpaidReturnCreatesNewOrder` green in inline mode; double-click + button-lock also green in classic mode (iframe flag flipped for one run, restored). Full `mollie-standard`: 11 passed + 2 Klarna skips + 3 new = green. Gates green in both repos (phpstan: payment-base's 20 pre-existing findings unchanged).
  - Env: e2e user `playwright.user@oxid-esales.dev` had a mangled address failing Mollie's validator — reset to demo values (`Marc` / `Hugo-Junkers Str` / `Frankfurt am Main`). OPC flag was already off. payment-base merged into `b-7.4.x` (`4b59482`, no-ff); Mollie CI pin back on `b-7.4.x` (a feature-branch pin cannot resolve `payment-base >=v1.2`).
  - Plan corrections: `disableContinueButton()` is not dead (matches apex's payment-step "Next"); double click reproduced as two sequential `page.request.post()` (same-tick submits collapse to one navigation); orders counted via DB (`fixtures/shop-db.ts`); controller reshaped for PHPMD (24 methods / complexity 49).
  - Follow-ups (out of scope): OPC server-side parity; phantom-row data cleanup (merchant); Stripe/PayPal replay glue.
- Sprint MOL-15 (user-data validation exactly as in Stripe) — **IMPLEMENTED**, 6 stories, branch `b-7.4.x-MOL-15-user-data-validation-parity`, CI green, **merged into `b-7.4.x` (`8a0cce2`, no-ff) after approval**. Plan `sprints/MOL-15-user-data-validation-parity.md`; `reports/MOL-15-user-data-validation-parity.md`.
  - Delivery-address pass + Stripe's field set (`postalCode`, phone trio) + `email`; describer extracted; all `MOLLIE_VALIDATION_*` translations added (module rendered raw keys before); `CheckoutUserDataGate` asked at the payment step and in `execute()` right before Mollie; OPC footer validates live fields via `cl=oepaymentvalidationapi` (inline `mollie-user-data-validator`); admin capture reason / refund description validated. Return leg of `MollieOrderController` moved into `HandlesMollieCheckoutReturn` (PHPMD).
  - Proof: e2e `MollieStandard/UserDataValidationBlocksCheckout` (payment step + order step) and `MollieOpc/UserDataValidation` green; unit 677; phpcs/phpstan/phpmd clean; `ModuleIndependenceTest` pins no Stripe reference. `mollie-standard` regression 16 passed + 2 Klarna skips.
- MOL-17 — refund action missing after a successful Mollie payment (reported for Pay by Bank / EPS) — investigated, sprint approved and **IMPLEMENTED** (Stories 1–6), branches `b-7.4.x-MOL-17-refund-after-paid-lost-update` in payment-base and mollie-payment, payment-base merged into `b-7.4.x` (`7ec6ed0`, no-ff, CI green on the branch tip); Mollie branch pushed, CI re-run against the merged payment-base, **merge awaits approval**. See `reports/MOL-17-refund-after-paid-lost-update.md`, `sprints/MOL-17-refund-after-paid-lost-update.md`.
  - Finding: not method-specific. Refund form needs contract `fulfilled` + Mollie `amountRemaining > 0`; Mollie returned a remainder for every method. The `committed`-but-paid orders are a **lost update**: `paid` webhook fulfils the contract (CAPTURE row written), the shopper's return leg then saves its stale in-memory copy (`committed`, `OXFULFILLEDAT NULL`) over it — no lock, no version, full-row UPDATE. 10 of 24 paid contracts on 2026-09-23/24 lost the race (iDEAL, PayPal, KBC here; Pay by Bank / EPS in the ticket).
  - Evidence: diagnostic `tests/MollieDiagnostic/RefundAvailabilityMatrix.spec.ts` (one paid order per offered method → DB + Mollie API + admin), DB timestamps, code trace of `MolliePanelViewDataBuilder::isRefundable`.
  - Done: optimistic `OXVERSION` on the contract row + `StaleContractException` (payment-base), return leg yields to the newer state, webhook retries once, `oe:mollie:reconcile-paid` command for stuck orders, captured amount on the paid path, `mollie:reconcile-paid` run on this shop fulfilled 13 stuck contracts. e2e `RefundAfterWebhookWinsRace` + `mollie-standard` regression: see report.
- Webhook retry after a failed delivery (follow-up of MOL-17, ticket id tbd) — **IMPLEMENTED**, branches `b-7.4.x-webhook-retry-after-failed-claim` in payment-base (fix) and mollie-payment (integration proof, docs); payment-base merged into `b-7.4.x` (`d2abe28`, no-ff); Mollie CI re-run against it, Mollie merge awaits approval. Plan `sprints/webhook-retry-after-failed-claim.md`. A `failed` claim is re-claimable by the PSP's retry; processed / in-flight claims stay exclusive; the failure reason is stored on the log row.
- MOL-10 (contract / order / transaction states) — REPORT, see `../20260923/reports/MOL-10-contract-order-transactioon-states.md`.
